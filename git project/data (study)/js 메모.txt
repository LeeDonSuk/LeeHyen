innerText =  element 안의 text 값들만을 가져옵니다.
innerHtml = element 안의 HTML이나 XML을 가져옵니다.
style = element 안의 css style 을 가져옵니다.
className = element 안의 className 을 가져옵니다.

const element = document.querySelector('div');
element.onclick function () = element 를 클릭했을때 실행할 함수를 작성한다. 


변수
var = 전역변수와 함수 내에서만 사용할수 있는 지역변수로 구분
let = 가변변수
const = 불변변수

호이스팅 = var 선언문 이용시 해당변수가 상위로 끌어올려지는 현상
ㄴ미리저장소에 등록하기 위함

스코프 
ㄴ로컬변수 => 중괄호 안에서 선언
ㄴ글로벌변수 =>  스크립트 상단에서 선언 ( 중가로 밖 )



`` = 빽틱기호 
ㄴ문자와 변수를 같이쓰는 방법
   ㄴ`이것은 문자 ${변수} 입니다.`

defer = 스크립트를 다운시켜놓고 HTML문서를 다읽은후 실행시킴


console.group = console.log의 그룹을 만들어서 보여준다.


//배열

선언방식 => new Array(), []
값의형식 => key :value
- 값을 이용하려면 key를 사용해야함

값넣기 
var a = new Array()'
a.push(10);
a.push(50);
a.push(800);

값출력
var a = [];
a.push(10);
a.push(20);
a.push(30);
console.log(a[0]); - 10
console.log(a[1]); - 20
console.log(a[2]); - 30

console.log(a.length); - 3 (배열갯수 출력)

1.forEach = 배열값을 모두 사용하기 위해 사용
2.배열값을 매개변수로 사용하능 (배열값,배열키,배열전체)[value,key,array]

a.forEach(function (v, k) {
total += v;
if (v === 50) {
console.log(`값 50 키는 ${v}`)
}
});


function 함수명 () {
    구현로직
} 선언식 함수


let 함수명 function  () {
    구현로직
} 표현식 함수